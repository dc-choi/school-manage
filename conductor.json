{
    "scripts": {
        "setup": "ln -sf \"$CONDUCTOR_ROOT_PATH/apps/api/.env.local\" apps/api/.env.local && ln -sf \"$CONDUCTOR_ROOT_PATH/apps/api/.env.test\" apps/api/.env.test && cp \"$CONDUCTOR_ROOT_PATH/apps/api/.env.local\" apps/api/.env && pnpm install && pnpm build",
        "run": "pnpm dev"
    },
    "runScriptMode": "nonconcurrent"
}
