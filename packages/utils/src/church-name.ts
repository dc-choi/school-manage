/**
 * 본당명 정규화
 *
 * 같은 교구 내 본당명 중복 여부를 판별할 때 사용한다.
 * - 모든 공백 문자(스페이스/탭/개행)를 제거
 * - 공백 위치/개수만 다른 입력은 동일 본당으로 간주 ("반포동 성당" === "반포동성당")
 * - 공백 외 글자가 다르면 항상 별개 본당 ("반포동성당" !== "반포4동성당")
 */
export const normalizeChurchName = (name: string): string => name.replace(/\s+/g, '');
